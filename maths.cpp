#include<iostream>
#include<cmath>
int main(){
    //double x=3;
   // double y=10;
    double z;
   // z=std::max(x,y);
   // z=std::min(x,y);
   // z=pow(2,4);
   // z=sqrt(9);
    z=round(3.33);
   // z=ceil(x); when x=3.34
   // z=floor(x); when x=3.94
   // z=abs(-24);
    std::cout<<z;

return 0;
}